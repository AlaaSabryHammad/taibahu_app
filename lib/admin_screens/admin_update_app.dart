import 'package:flutter/material.dart';

class AdminUpdateApp extends StatelessWidget {
  const AdminUpdateApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}